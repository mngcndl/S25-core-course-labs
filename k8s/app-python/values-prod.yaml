replicaCount: 3
image:
  repository: mangocandle/app_python
  tag: latest
  pullPolicy: IfNotPresent
resources:
  limits:
    cpu: "1"
    memory: "1024Mi"
  requests:
    cpu: "0.5"
    memory: "512Mi"

probes:
  liveness:
    path: /
    port: http
    initialDelaySeconds: 10
    timeoutSeconds: 5
    periodSeconds: 10
    failureThreshold: 3
  readiness:
    path: /
    port: http
    initialDelaySeconds: 10
    timeoutSeconds: 5
    periodSeconds: 10
    failureThreshold: 3
